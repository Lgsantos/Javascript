function soma(n1=0, n2=0) {//ao pré-definir os parâmetros formais, protegemos o código em caso de não envio de valores para esses parâmetros na chamada da função
    return n1 + n2
}

console.log(soma(2,5))